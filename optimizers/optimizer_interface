from abc import abstractmethod

class OptimizerInterface():
    
    @abstractmethod
    def optimize(self, data):
        pass

class SgdOptimizer(OptimizerInterface):

    def optimize(self, data):
        for i in range(len(data)):
            data[i] -= self.learning_rate * data[i].grad
        return data